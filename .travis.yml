language: python
python:
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install coveralls
script: 
  - nosetests --with-coverage --cover-package=app tests/
  - coveralls
branches:
  except:
  - master
  only:
  - develop
after_success:
 - coveralls
notifications:
  email: false
